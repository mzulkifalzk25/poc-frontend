import { PlaceholderPage } from "~/components/ui/PlaceholderPage";

export const handle = { title: "Held bills" };

export default function HeldRoute() {
  return <PlaceholderPage title="Held bills" />;
}
